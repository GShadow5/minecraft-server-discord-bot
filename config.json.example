{
    "config": {
        "chat_channel": "testchatchannel",
        "command_channel": "testchatchannel",
        "command_prefix": "!",
        "regex": [
            {
                "match": ".*\\]: <(.*)> (.*)",
                "fstring": "**[{groups[0]}]**: {groups[1]}"
            }
        ]
    }
}